<app-navigation>

  <div class="card">
    <div class="card-header">
      <code>Add task to <strong>"{{toDoListItemArray.title}}"</strong> </code>
    </div>
    <div class="card-body">
      <form #createToDoForm="ngForm" (ngSubmit)="createTodoItem()">
        <div class="form-group">
          <label for="title">To Do Item Description:</label>
          <input type="text" name="title" [(ngModel)]="title" #name="ngModel" id="title" required class="form-control"
            [ngClass]="name.valid && (name.dirty || name.touched) ? 'is-valid text-capitalize' : 'is-invalid text-capitalize'"
            id="itemDescription" />
        </div>
        <button type="submit" class="btn btn-success" [disabled]="!createToDoForm.form.valid">Save Item</button>

        <button type="button" class="btn btn-warning pull-right" (click)="goBackToPreviousPage()">Go Back</button>

      </form>
      <ul class="list-group" *ngIf="toDoListItemArray">
        <li class="list-group-item mt-2" *ngFor="let todo of toDoListItemArray">
          <span class="hover-success" [class.text-success]="todo.isComplete">
            <i class="fa fa-lg" [ngClass]="todo.isComplete ? 'fa fa-check-circle-o' : 'fa fa-circle-thin'"></i>
          </span>
          {{todo.title}}
          <span class="hover-cursor text-danger pull-right" placement="top" ngbTooltip="Delete">
            <i class="fa fa-trash-o fa-lg"></i>
          </span>
          <span class="hover-cursor text-success pull-right" [routerLink]="['/subtodo', todo.listId, todo.taskId]"
            placement="top" ngbTooltip="Create sub-todo item">
            <i class="fa fa-plus-circle fa-lg mr-2"></i>
          </span>
          <span *ngIf="!todo.isComplete" class="hover-cursor text-warning pull-right mr-2" placement="top"
            ngbTooltip="Edit item">
            <i class="fa fa-pencil fa-lg" (click)="editList(todo)"></i>
          </span>
        </li>
      </ul>
    </div>
  </div>
</app-navigation>
<!-- <ngx-spinner bdColor="rgba(51,51,51,0.8)" size="small" color="#87c4a3" type="ball-spin-clockwise-fade-rotating">
  <p style="font-size: 14px; color: #ccc">Loading...</p>
</ngx-spinner> -->



<router-outlet></router-outlet>s